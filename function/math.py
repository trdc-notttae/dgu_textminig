
def ty_sum(sample_list):
    sum=0
    for num_in_list in sample_list:
        sum=sum+num_in_list
    return(sum)

def ty_multi(sample_list):
    result=0
    for num_in_list in sample_list:
        result=result*num_in_list
    return(result)